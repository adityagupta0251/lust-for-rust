/*
#[derive(Debug)]
struct Rectangle {
    width: u32,
    height: u32,
        
}

impl Rectangle {
    // In Rust, the impl (implementation) keyword is used to define the functionality of a type. It serves two primary purposes: defining methods for a specific type and implementing shared behaviors (traits) for a type.
    fn area(&self) -> u32 {
        self.width * self.height
    }
}


fn main() {
    let rect1 =  Rectangle {
        width: 30,
        height: 50,
        
    };
    println!(
        "The area of the rectangle is {} square pixels.",
        rect1.area()
    );
}

*/

// Self Keyword
/*
impl Rectangle {
    fn width(&self) -> bool {
        self.width > 0
        
    }
}

fn main() {
    let rect1 = Rectangle {
        width: 30,
        height: 50,
        
    };

    if rect1.width() {
        println!("The rectangle has a nonzero width; it is {}", rect1.width);
    }
}
*/



